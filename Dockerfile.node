# Dockerfile para o servidor de upload em Node.js
FROM node:18-slim

LABEL maintainer="Gemini"
LABEL description="Dockerfile for the Node.js upload server with image processing."

WORKDIR /app

# Instalar dependências de produção
COPY package.json package-lock.json* ./
RUN npm install --production

# Copiar o código da aplicação
COPY upload-server.js .
COPY public/ ./public/

# Expor a porta que o servidor usa
EXPOSE 5000

# Comando para iniciar o servidor
CMD ["node", "upload-server.js"]
