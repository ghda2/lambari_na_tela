# Caddyfile

# Ambiente local: atende qualquer host em :80 (ex.: http://localhost)
:80 {
    reverse_proxy app:3000
}

# Produção: substitua pelo seu domínio para HTTPS automático
# exemplo.com {
#     reverse_proxy app:3000
# }